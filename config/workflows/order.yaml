name: "order"
auditTrail:
    enabled: true
type: "state_machine"
stateField: "state"
initialState: ["draft"]
places:
    draft: {}
    pending: {}
    confirmed: {}
transitions:
    initiate:
        from: ["draft"]
        to: ["pending"]
    confirm:
        from: ["pending"]
        to: ["confirmed"]